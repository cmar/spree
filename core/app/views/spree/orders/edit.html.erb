<% @body_id = 'cart' %>

<h1><%= t(:shopping_cart) %></h1>

<% if @order.line_items.empty? %>

  <div data-hook="empty_cart">
    <p><%= t(:your_cart_is_empty) %></p>
    <p><%= link_to t(:continue_shopping), products_path, :class => 'button continue' %></p>
  </div>

<% else %>
  <div data-hook="outside_cart_form">
    <%= form_for @order, :url => update_cart_path, :html => {:id => 'update-cart'} do |order_form| %>
      <div data-hook="inside_cart_form">

        <div data-hook="cart_items">
          <%= render :partial => 'form', :locals => { :order_form => order_form } %>
        </div>

        <div id="subtotal" data-hook>
          <h3><%= t(:subtotal) %>: <%= order_subtotal(@order) %></h3>
          <div class="links" data-hook="cart_buttons">
            <%= link_to t(:continue_shopping), products_path, :class => 'checkoutButtonContinue checkout primary' %>
            <%= button_tag :class => 'button', :id => 'update-button' do %>
              <%=  t(:update) %>
            <% end %>
            <%= link_to t(:checkout), checkout_path, :class => 'checkoutButton checkout primary', :id => 'checkout-link' %>
          </div>
       </div>

      </div>
    <% end %>
  </div>
  <div id="empty-cart" data-hook style="float:left;">
   <%= form_tag empty_cart_path, :method => :put do %>
    <p id="clear_cart_link" data-hook>
      <%= submit_tag t(:empty_cart), :class => 'button' %>
    </p>
  <% end %>
  </div>
<% end %>
